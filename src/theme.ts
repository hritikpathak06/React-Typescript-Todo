// src/theme.ts
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#fc035a',
    },
    // secondary: {
    //   main: '#f50057',
    // },
  },
});

export default theme;
